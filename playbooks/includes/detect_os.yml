---
# playbooks/includes/detect_os.yml
# Shared pre-task to add host to OS-specific group

- name: Add host to OS-specific group
  ansible.builtin.group_by:
    key: "{{ 'macos' if ansible_facts['os_family'] == 'Darwin' else 'linux' }}"
  tags: [always]
